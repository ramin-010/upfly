# An entity-encoded extension

A browser resolves this to a real picture. Nothing else in this document names a file, so
a pass that decides whether to read a document at all has nothing here to save it.

![Entity-encoded dot](/img/screenshot&#46;png)
